# Phase 3 placeholder — replaced with full implementation in Phase 3
FROM python:3.11-slim
WORKDIR /app
CMD ["python", "-c", "import time; print('Producer placeholder — implement Phase 3'); time.sleep(86400)"]
